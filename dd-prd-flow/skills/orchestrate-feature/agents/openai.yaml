interface:
  display_name: "DD Orchestrate PRD Flow"
  short_description: "Detect 当前阶段 and run the next PRD Flow step from existing docs"
  default_prompt: "使用 $orchestrate-feature，判断并执行当前项目下一步 PRD 工作流。"
